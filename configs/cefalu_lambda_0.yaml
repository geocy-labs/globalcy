run_name: cefalu_lambda_0
bundle_path: "../geo-cy-data/outputs/cefalu_lambda_0_0"
model:
  type: global
  hidden_dims: [64, 64]
training:
  seed: 7
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  train_fraction: 0.8
loss:
  target_weight: 1.0
  determinant_weight: 0.1
  positivity_weight: 0.05
  chart_weight: 0.0
  symmetry_weight: 0.0
evaluation:
  projective_scale: 1.7
