run_name: quartic_fermat_smoke
bundle_path: "../geo-cy-data/outputs/globalcy_ferm_smoke"
model:
  type: local
  hidden_dims: [64, 64]
training:
  seed: 7
  epochs: 10
  batch_size: 32
  learning_rate: 0.001
  train_fraction: 0.8
loss:
  target_weight: 1.0
  determinant_weight: 0.1
  positivity_weight: 0.05
  chart_weight: 0.0
  symmetry_weight: 0.0
evaluation:
  projective_scale: 1.7
